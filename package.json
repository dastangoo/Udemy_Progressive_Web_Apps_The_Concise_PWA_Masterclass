{
  "name": "push_server",
  "version": "1.0.0",
  "description": "Web Push Server",
  "main": "server/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "nodemon server/",
    "vapid": "node_modules/.bin/web-push generate-vapid-keys --json > server/vapid.json"
  },
  "keywords": [
    "pwa",
    "push-server"
  ],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "urlsafe-base64": "^1.0.0",
    "web-push": "^3.3.3"
  }
}
